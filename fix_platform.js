const fs = require('fs');\nlet c = fs.readFileSync('components/layout/AppShell.tsx', 'utf8');\nconst insert = [\n  '  useEffect(() => {',\n  '    if (typeof window !== ''undefined'' && (window as any).Capacitor) {',\n  '      document.documentElement.setAttribute(''data-platform'', ''ios'');',\n  '    }',\n  '  }, []);',\n  '',\n].join('\n');\nc = c.replace('const refreshProfile = useCallback', insert + 'const refreshProfile = useCallback');\nfs.writeFileSync('components/layout/AppShell.tsx', c, 'utf8');\nconsole.log('matched:', c.includes('data-platform'));